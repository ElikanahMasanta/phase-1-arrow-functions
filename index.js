function divide(number, number1){
    return 20;
}
divides(2000,100)

const square = (nummber,number1) => (x ** 2); 
square;

const add = (parameter1, parameter2) => parameter1 + parameter2;
add(2,3);
